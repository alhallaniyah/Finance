# Frontend (Vite) env vars — safe to expose
# Base URL for the local Flask API
VITE_API_BASE_URL=http://localhost:5001

# Server-only env vars — do NOT prefix with VITE_
# Place these in your Flask environment (.env or shell), not the frontend.
GOOGLE_CLIENT_ID=1020053530014-94gni8e6m47paiq3498e1h7cv6cra3cq.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=REPLACE_WITH_YOUR_SECRET
GOOGLE_PROJECT_ID=project-23430013-2003-4c61-abc
GOOGLE_AUTH_URI=https://accounts.google.com/o/oauth2/auth
GOOGLE_TOKEN_URI=https://oauth2.googleapis.com/token
GOOGLE_CERTS_URL=https://www.googleapis.com/oauth2/v1/certs
GOOGLE_REDIRECT_URI=http://localhost:5001/api/google/oauth/callback
GOOGLE_CALENDAR_SCOPE=https://www.googleapis.com/auth/calendar.events

# Flask server port (the frontend calls http://localhost:5001 by default)
PORT=5001

# ESC/POS printer configuration (server-side)
# Set these in your Flask server environment. Do NOT prefix with VITE_.
ESC_POS_CONNECTION=network
ESC_POS_HOST=192.168.70.124
ESC_POS_PORT=9100
ESC_POS_VENDOR_ID=0x0000
ESC_POS_PRODUCT_ID=0x0000
ESC_POS_USB_IN_EP=0x82
ESC_POS_USB_OUT_EP=0x01
ESC_POS_SERIAL_PORT=/dev/ttyUSB0
ESC_POS_SERIAL_BAUD=19200